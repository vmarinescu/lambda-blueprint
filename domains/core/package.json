{
  "name": "@serverless-blueprint/core",
  "version": "0.0.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist node_modules",
    "test:ut": "jest ut",
    "test:it": "jest it --testTimeout=10000 --passWithNoTests",
    "test:e2": "jest e2 --testTimeout=10000 --passWithNoTests",
    "prepare": "tsc"
  }
}
