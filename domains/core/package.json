{
  "name": "@serverless-blueprint/core",
  "version": "0.0.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist node_modules",
    "test": "jest unit",
    "test:int": "jest int --testTimeout=10000 --passWithNoTests",
    "test:e2e": "jest e2e --testTimeout=10000 --passWithNoTests",
    "prepare": "tsc"
  }
}
