{
  "name": "@serverless-blueprint/core",
  "version": "0.0.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist node_modules",
    "test:ut": "jest /ut --passWithNoTests --setupFiles dotenv/config",
    "test:it": "jest /it --passWithNoTests --setupFiles dotenv/config",
    "test:ee": "jest /ee --passWithNoTests",
    "prepare": "tsc"
  }
}
