{
  "name": "@serverless-blueprint/handover",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist node_modules",
    "test:unit": "jest unit",
    "test:int": "jest int --testTimeout=10000",
    "test:e2e": "jest e2e --testTimeout=10000"
  },
  "dependencies": {
    "@serverless-blueprint/core": "^0.1.0",
    "uuid": "^8.0.0"
  },
  "version": "0.1.0"
}
